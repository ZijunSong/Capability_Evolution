# SCAPE H20 下一步：Candidate-B True-SCAPE Micro-Learnability Tournament

## 当前结论

`evidence_graph`：

```text
Contribution positive
Influence positive
Placement = semantic-migratable / hybrid
true-SCAPE Learnability = FAIL × 2
```

因此：

```text
停止 full evidence_graph migration
不进 Stage S
不再扫 lr / LoRA / seed
```

新的核心问题：

> `subtractive_curation / importance_tagging / verify_tool` 中，谁真正能被 same-state tool-token OPD 学进 weights？

当前证据排序不一致：

```text
Utility:
subtractive > importance > verify

Influence:
verify > importance > subtractive

Contribution:
subtractive 最稳定
importance split-sensitive
verify local neutral
```

所以本轮先做统一 micro-learnability tournament，不手工冻结 B。

---

## 0. Repo

```text
/data/ppnm/Capability_Evolution/SCAPE
```

开始：

```bash
git status -sb
git fetch origin
pytest -q
```

确认：

```text
legacy_scope_path_used=false
```

---

## 1. 三候选统一数据

```text
SC = subtractive_curation
IT = importance_tagging
VT = verify_tool
```

每个：

```text
TRAIN >= 2K states
VALID >= 512
TEST >= 512
```

必须：

```text
query-disjoint
H_-m Student rollout 决定 state occupancy
same xi_t dual-view
teacher score-only
no future trajectory
```

VT 分：

```text
natural
targeted-eligible
```

但 Gate L 主结果只用 natural。

---

## 2. 第一阶段只跑到 2K

不要直接 8K。

每 candidate：

```text
L512
L2K
```

两 seed：

```text
42
43
```

所有 cell 从同一 base checkpoint 独立初始化。

统一 loss：

```text
uniform name+args tool-token KL
+ light anchor KL
```

---

## 3. 8 卡并行

### GPU0
```text
SC seed42:
L512 -> heldout
L2K -> heldout
```

### GPU1
```text
SC seed43:
L512 -> heldout
L2K -> heldout
```

### GPU2
```text
IT seed42:
L512 -> heldout
L2K -> heldout
```

### GPU3
```text
IT seed43:
L512 -> heldout
L2K -> heldout
```

### GPU4
```text
VT seed42:
L512 -> heldout
L2K -> heldout
```

### GPU5
```text
VT seed43:
L512 -> heldout
L2K -> heldout
```

### GPU6
```text
SC same-state action-CE baseline @2K
```

### GPU7
```text
SC tool-name-only KL @2K
```

若单卡不能稳定训练 20B：

```text
GPU0-1: SC seed42 -> seed43
GPU2-3: IT seed42 -> seed43
GPU4-5: VT seed42 -> seed43
GPU6-7: SC action-CE -> name-only
```

---

## 4. Micro Gate

每 candidate：

```text
D_pre
D_post_512
D_post_2K
L_m_512
L_m_2K
name KL
arg-key KL
arg-value KL
invalid tool rate
syntax smoke
```

### MICRO_PASS
要求：

```text
两 seed 方向一致
D_post_2K < D_pre
2K 不比 512 系统性退化
invalid tool 不升
```

### MICRO_WEAK
一个 seed 正、一个近零：

```text
最多允许一次 attribution-guided 2K retry
```

### MICRO_FAIL
两 seed失败或 scaling regression：

```text
停止该 component
不跑 8K
```

---

## 5. 冻结 Candidate B

只在 micro 结果后冻结。

规则：

```text
1. 过滤 MICRO_FAIL
2. MICRO_PASS 中优先 live utility 稳定者
3. influence 用于解释/weighting，不覆盖 utility
4. placement sanity 必须通过
```

输出：

```text
outputs/true_scape_candidate_b_tournament/CANDIDATE_B_FINAL.json
```

---

## 6. 只扩 winner 到 8K

winner = W。

### GPU0
```text
W L8K seed42
```

### GPU1
```text
W L8K seed43
```

### GPU2
```text
W L8K seed44
```

### GPU3
```text
W name-only L8K
```

### GPU4
```text
W args-only L8K
```

### GPU5
```text
W action-CE L8K
```

### GPU6
```text
W full-response KL L8K
```

### GPU7
```text
W off-policy matched L8K
```

只有 MICRO_PASS 才启动。

---

## 7. Attribution-Guided Retry

若 winner uniform L8K FAIL：

只允许一次 retry。

读取对应 H100 attribution：

```text
subtractive:
H1003_SUBTRACTIVE_HANDOFF.json

importance:
已有 H100-3 attribution

verify:
已有 H100-3 attribution
```

只允许一次。

仍 FAIL：

```text
CURRENTLY_NOT_LEARNABLE
```

换下一个 MICRO_PASS candidate。

---

## 8. Stage S

只有 winner Stage L PASS。

四格：

```text
S0 theta0 + H_full
S1 theta0 + H_-W
S2 thetaW + H_-W
S3 thetaW + H_full
```

至少：

```text
200 query-disjoint tasks
```

报告：

```text
curated recall
trajectory recall
final recall
reward
tool calls
context tokens
latency
component runtime cost
CCR
N_post
```

---

## 9. Evidence Graph 后续

等待：

```text
H1001_GRAPH_HYBRID_HANDOFF.json
```

如果：

```text
graph state retained
minimal render sufficient
```

则 Evidence Graph 只作为后续 hybrid case：

```text
theta + graph_state + minimal_render
```

不再做 full graph internalization。

---

## 10. Probe Validation V2

必须生成：

```text
PROBE_VALIDATION_V2.md
```

表：

```text
component
Contribution
Influence
Utility
Learnability
Placement
```

重点比较：

```text
evidence_graph:
C+ I+ L-

subtractive:
C+ I+ Ustrong -> L?

importance:
Cmixed I+ Umid -> L?

verify:
C0 Istrong Uweak -> L?
```

据此判断 Pre-stage 是否应该正式升级为：

```text
Contribution–Influence–Utility–Learnability
```

---

## 11. 输出

```text
outputs/true_scape_candidate_b_tournament/
  DATA_AUDIT.md
  MICRO_STAGE_L.csv
  MICRO_STAGE_L_REPORT.md
  CANDIDATE_B_FINAL.json
  CANDIDATE_B_FINAL.md
  WINNER_8K_REPORT.md
  BASELINE_COMPARISON.md
  FOUR_GRID_STAGE_S.md
  PROBE_VALIDATION_V2.md
  RUN_MANIFEST.json
```

---

## 12. 禁止事项

```text
不继续 evidence_graph full migration
不启动 Stage M
不同时把三个 candidate 都跑到 8K
不使用旧 SCOPE OPD
不使用 capability-specific KEEP/SKIP labels
不把 verify targeted states 混入 natural Gate L
```
