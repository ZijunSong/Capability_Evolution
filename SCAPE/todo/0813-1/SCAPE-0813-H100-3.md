# SCAPE H100-3 下一步：Subtractive-Curation Influence Attribution + Probe Analysis

## 任务定位

当前 Candidate-B utility 把：

```text
subtractive_curation
```

排第一，但 H100-3 还没有给它做与 evidence_graph / importance_tagging / verify_tool 同等级的 attribution。

本机下一步优先复用现有：

```text
REAL_INFLUENCE_PER_STATE.jsonl
```

回答：

> subtractive_curation 的 influence 主要落在哪些 tool、turn 和 argument token 上？

同时做一个 Probe predictive table，开始判断 SCAPE 是否应从三轴升级为四轴。

---

## 0. Repo / Git

```text
/mnt/songzijun/Capability_Evolution/SCAPE-wt-h100-3
```

分支：

```text
exp/h1003-subtractive-attribution
```

开始：

```bash
git status -sb
git fetch origin
pytest -q
```

继续负责公共 scorer/aggregation 代码 hygiene：

```text
代码改动可 commit + push
outputs/models/indexes 不提交
```

---

## 1. 输入

优先：

```text
outputs/h100_3_real_influence/REAL_INFLUENCE_PER_STATE.jsonl
```

目标：

```text
subtractive_curation
```

对照：

```text
importance_tagging
verify_tool
```

---

## 2. 分层

每 state 标记：

```text
turn:
early / middle / late

student tool
teacher preferred tool

difference:
tool-name change
args-only
no meaningful change

argument class:
add_ids
remove_ids
doc_ids
importance
query
claim
termination reason
```

---

## 3. 统计

输出：

```text
I_name by tool
I_args by argument class
tool-name disagreement
args-only disagreement
teacher entropy
student entropy
high-influence archetypes
```

重点回答：

```text
是否主要影响 curate？
是否主要影响 add/remove ids？
tool name 是否基本不变？
是否集中在 late-turn？
```

---

## 4. GPU 使用

优先 CPU 聚合。

如果现有 JSONL 已足够：

```text
GPU0-7 不启动
```

如果缺 token-level continuation score，再补：

### GPU0-1
```text
subtractive top-influence 256 states
```

### GPU2-3
```text
subtractive random 256 states
```

### GPU4-5
```text
importance matched-control 256
```

### GPU6
```text
null perturbation
```

### GPU7
```text
deterministic replay
```

---

## 5. Probe Predictive Analysis

生成：

```text
PROBE_PREDICTIVE_TABLE.csv
```

至少：

```text
evidence_graph:
C+ I+ U? L-

subtractive:
C+ I+ Ustrong L?

importance:
Cmixed I+ Umid L?

verify:
C0 Istrong Uweak L?
```

目标是分析：

```text
Contribution–Influence–Learnability
```

是否应升级为：

```text
Contribution–Influence–Utility–Learnability
```

暂不把它写死为最终方法公式。

---

## 6. H20 Handoff

生成：

```text
H20_SUBTRACTIVE_LOSS_RECOMMENDATION.md
```

但首轮 H20 tournament 仍保持：

```text
uniform name+args KL
```

本报告只决定：

```text
如果 uniform fail
第二次 attribution-guided retry 偏 name 还是 args
```

每个新 component 最多：

```text
uniform
+
一次 retry
```

---

## 7. 输出

```text
outputs/h100_3_subtractive_attribution/
  SUBTRACTIVE_ATTRIBUTION.md
  INFLUENCE_BY_TOOL.csv
  INFLUENCE_BY_ARGUMENT_CLASS.csv
  INFLUENCE_BY_TURN.csv
  HIGH_INFLUENCE_ARCHETYPES.jsonl
  H20_SUBTRACTIVE_LOSS_RECOMMENDATION.md
  PROBE_PREDICTIVE_TABLE.csv
  PROBE_PREDICTIVE_NOTE.md
```

handoff：

```text
outputs/scape_prestage_v3/H1003_SUBTRACTIVE_HANDOFF.json
```

---

## 8. 禁止事项

```text
不训练
不重跑全 influence
不继续 evidence_graph Stage L
不单独修改 candidate ranking
```
