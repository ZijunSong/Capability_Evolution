# SCAPE H100-1 下一步：Evidence-Graph Hybrid Runtime Pareto（无需训练）

## 任务定位

H100-1 已经得到：

```text
HYBRID_TARGET_CONFIRMED
I_12=0.016256
I_23=0.044908
I_34=0.027175
```

且 G3=`GRAPH_STATE_PLUS_MINIMAL_RENDER` 接近 G4=`GRAPH_FULL_RENDER`。

因此即使 H20 Learnability 尚未建立，本机仍可独立回答：

> 不训练模型，仅把 full graph renderer/controller 缩到 minimal render，能否获得 quality-preserving runtime reduction？

这直接验证 SCAPE 的 placement/recomposition，不依赖 OPD。

## 0. Repo

```text
/mnt/songzijun/Capability_Evolution/SCAPE-wt-h100-1
```

分支：

```text
exp/h1001-graph-runtime-pareto
```

## 1. 条件

固定 released Harness-1 checkpoint，只比较：

```text
G1 GRAPH_OFF
G2 GRAPH_STATE_ONLY
G3 GRAPH_STATE_PLUS_MINIMAL_RENDER
G4 GRAPH_FULL_RENDER
```

## 2. Fresh evaluation

冻结：

```text
GRAPH_PARETO400
seed=1125
n=400
```

与此前 graph decomposition / influence split query-disjoint。

400 完成并过 gate 后，才允许扩 `GRAPH_PARETO830`。

## 3. 8 卡并行

### GPU0-1
```text
G4 FULL_RENDER，两 shard
```

### GPU2-3
```text
G3 MINIMAL_RENDER，两 shard
```

### GPU4
```text
G2 STATE_ONLY
```

### GPU5
```text
G1 GRAPH_OFF
```

### GPU6
```text
runtime profiler:
context tokens
state ops
serialization bytes
latency
memory bytes
```

### GPU7
```text
paired bootstrap + quality-cost Pareto aggregation
```

## 4. 指标

Quality：

```text
curated recall
trajectory recall
final recall
reward
```

Cost：

```text
rendered graph tokens
total context tokens
graph state ops
latency
memory bytes
tool calls
wall-clock
```

paired：

```text
G3 vs G4
G2 vs G4
G1 vs G4
```

## 5. Gate

### MINIMAL_RENDER_PARETO_PASS
若：

```text
G3 quality non-inferior to G4
且 G3 cost < G4
```

则得到无需训练的结论：

```text
full graph renderer 不是必要 runtime；
minimal external graph representation 已实现 Pareto improvement。
```

### STATE_ONLY_TOO_WEAK
若 G2 明显低于 G3，进一步支持“semantic render/decision surface 不能完全删除”。

## 6. 与 H20 的关系

本机不等待 H20 metric audit，因为本实验不使用 Learnability metric，也不训练模型。

## 7. 输出

```text
outputs/h100_1_graph_runtime_pareto/
  GRAPH_PARETO_PER_QUERY.jsonl
  GRAPH_PARETO_SUMMARY.csv
  PAIRED_BOOTSTRAP.json
  QUALITY_COST_PARETO.md
  MINIMAL_RENDER_DECISION.json
  RUN_MANIFEST.json
  SHA256SUMS
```

## 8. 禁止事项

```text
不训练
不救 evidence_graph OPD
不做 Learnability 判定
不声称 official Chroma parity
```
