# SCAPE H100-4 下一步：Exact Utility Reproduction（4×H100）

## 资源约束

**本机只有 4 张 H100。**

本轮只做 H100-2 `UTILITY_COMMON128` 的精确复现，目的是检查跨机器 reproducibility，而不是再做新 split。

## 0. 前置依赖

等待 H100-2 生成：

```text
H1004_EXACT_REPLAY_HANDOFF.json
UTILITY_COMMON128_MANIFEST.json
```

正式文件未出现前，不启动 GPU run；只做环境和脚本检查。

## 1. Repo / 环境

```text
/mnt/songzijun/Capability_Evolution/SCAPE-wt-h100-4
```

分支：

```text
audit/h1004-utility-exact-replay
```

GPU env：

```text
/opt/scape-hf-scorer/bin/python
```

禁止 `/mnt` JuiceFS env。

## 2. 必须精确复现

禁止重新：

```text
抽 query
抽 state
挑高 divergence state
改变 K
改变 scorer
改变 retrieval corpus
改变 runner 参数
```

完全读取 H100-2 manifest：

```text
query_id
snapshot_hash
a_S
a_T
component
K
seed
```

## 3. 4 卡调度

### GPU0
```text
SC K2 -> SC K4
```

### GPU1
```text
IT K2 -> IT K4
```

### GPU2
```text
VT K2 -> VT K4
```

### GPU3
```text
same-action replay noise -> aggregation
```

若单任务必须 TP=2：

```text
GPU0-1: SC K2/K4 -> IT K2/K4
GPU2-3: VT K2/K4 -> replay noise
```

绝不超过 4 卡。

## 4. Cross-Machine Reproducibility

逐 state 对齐：

```text
state_id
utility_h1002
utility_h1004
difference
```

报告：

```text
Pearson
Spearman
mean absolute difference
sign agreement
top-k agreement
component ranking agreement
```

## 5. 判定

### MACHINE_REPRODUCIBLE
若：

```text
per-state 高相关
component mean 同方向
ranking 基本一致
```

说明此前差异主要来自 split/state selection。

### MACHINE_SENSITIVE
若完全相同 manifest 仍显著不同：优先排查 environment、model/scorer revision、retrieval backend、floating-point/runtime、fork implementation。此时 Utility probe 暂停作为 selector。

## 6. 输出

```text
outputs/h100_4_utility_exact_replay/
  EXACT_REPLAY_PER_STATE.csv
  CROSS_MACHINE_AGREEMENT.json
  CROSS_MACHINE_AGREEMENT.md
  REPLAY_NOISE.csv
  UTILITY_REPRO_DECISION.json
  RUN_MANIFEST.json
  SHA256SUMS
```

## 7. 禁止事项

```text
不做新 split
不训练
不重跑 verify influence
不超过 4 卡
不自行改变 H100-2 manifest
```
