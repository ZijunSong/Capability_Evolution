# H100-2 下一步：Independent 10-Component Replication

## 角色

最新记录中的 H100-2 是：

```text
4 modules + 6 coalition rows
且主要来自 frozen SCOPE outputs consolidation
```

这不能替代原计划的：

> **在 SCAPE/Harness-1 当前代码上，独立 fresh split 的 full + 10-component LOO replication。**

本机下一步专门补这个缺口。

---

# 0. Git barrier

禁止在 shared root 上 checkout。

H100-3 完成 Git barrier 后使用：

```text
/mnt/songzijun/Capability_Evolution/SCAPE-wt-h100-2
branch exp/h1002-independent-repl
```

---

# 1. Freeze Independent Split

创建：

```text
BCP_REPL200_V2
n=200
seed=2203
```

必须与：

```text
H100-1 CAL200
H100-1 CONFIRM400
H20 CAL64
```

query-disjoint。

若 H100-1 CONFIRM400 manifest 已存在，自动读取排除；
若尚未存在，使用预先协调的 stable-hash bucket，保证两个 split 不会冲突。

写：

```text
manifests/bcp_repl200_v2.json
manifests/REPLICATION_SPLIT_AUDIT.md
```

---

# 2. Runtime

统一：

```text
pat-jj/harness-1 checkpoint
local_bm25_compat
T=0
same context
same max turns
same tool budget
same scorer
```

本轮不读取旧 SCOPE `h100_2_module_utility` 作为实验输入。

旧输出只能在最终报告中作为历史对比。

---

# 3. 8 卡并行队列

## GPU0

```text
R0 full / REPL200
完成后：
R0b full / first 40q replay parity
```

## GPU1

```text
R1 - subtractive_curation
R2 - importance_tagging
```

## GPU2

```text
R3 - auto_populate_first_search
R4 - token_budget_marker
```

## GPU3

```text
R5 - evidence_graph
R6 - chunk_neighbors
```

## GPU4

```text
R7 - sentence_compress
R8 - content_dedup
```

## GPU5

```text
R9 - verify_tool
R10 - adaptive_rerank_instruction
```

## GPU6

主 LOO 全部完成后再执行：

```text
K1 coalition:
  - evidence_graph
  - subtractive_curation

K2 coalition:
  - importance_tagging
  - subtractive_curation
```

若主 LOO 尚未齐，GPU6 可 work-steal GPU1-5 的第二个队列，但输出 id 不变。

## GPU7

主 LOO 全部完成后：

```text
K3 coalition:
  - evidence_graph
  - chunk_neighbors

K4 coalition:
  - auto_populate_first_search
  - adaptive_rerank_instruction
```

同样允许主 LOO work-steal 优先。

---

# 4. 输出

```text
outputs/h100_2_independent_repl/
  LOO_REPLICATION_V2.csv
  LOO_REPLICATION_V2.json
  COALITION_V2.csv
  PLACEMENT_STABILITY_V2.md
  CROSS_SPLIT_COMPARISON.md
  RUN_MANIFEST.json
  STATUS_LIVE.md
  SHA256SUMS
```

每个 component 报：

```text
Δ curated recall
Δ trajectory recall
Δ final recall
Δ reward
Δ turns
Δ tool calls
Δ rendered tokens
paired W/L/T
bootstrap CI
```

---

# 5. Replication Gate

与 H100-1 比较：

```text
H100-1 CAL200
H100-1 CONFIRM400（若已完成）
H100-2 REPL200_V2
```

分类：

```text
STABLE_POSITIVE
SPLIT_SENSITIVE
DOMAIN/RENDER_SENSITIVE
NEUTRAL
NEGATIVE
```

重要：

```text
verification
```

旧 H100-2 consolidation 中看起来很强，但 H100-1 local LOO 为零。

因此本次 `verify_tool` 是重点冲突消解对象；不要因旧结果先验而调整设置。

---

# 6. Coalition

定义：

```text
interaction =
  Δ(m1,m2) - Δ(m1) - Δ(m2)
```

只把 coalition 当作：

```text
后续 Harness dropout 是否需要 joint removal
```

的调度依据。

不把 200q coalition 写成强因果 claim。

---

# 7. Stop Rule

当：

```text
full + 10 LOO
```

全部 quality-complete 后，本机主要任务即完成。

不要继续：

```text
训练
OPD
旧 finalization
旧 SCOPE prompt experiments
```

除非 H20 后续明确要求针对 top candidate 增加某个 fresh confirm。
