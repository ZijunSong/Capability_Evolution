# SCAPE-0814-H100-2.md
## H100-2（8×H100）下一步：4-Component Long-Horizon Teacher-Action Value

> H20 已证明：Contribution / Influence 阳性并不意味着 learnability。
>
> 下一步在 Pre-stage 中必须回答一个此前没有严格回答的问题：
>
> **Full-Harness teacher 改变的动作，到底是否真的比 Reduced-Harness student 的动作更有 downstream value？**
>
> 本机只测 teacher-action value，不训练模型。

---

# 1. 先检查是否已有上一轮 long-horizon 产物

检查：

```text
outputs/h100_2_long_horizon_advantage/
```

若已有完整：

```text
SC / IT / VT K4/K8 + replay noise
```

则不要重复，直接补：

```text
evidence_graph K4/K8
terminal confirm
```

若尚未运行，则按本文件完整执行。

Repo：

```bash
cd /mnt/songzijun/Capability_Evolution/SCAPE-wt-h100-2/SCAPE
```

Python 必须 `/opt`。

---

# 2. 本轮组件

四个 semantic/hybrid component：

```text
evidence_graph
verify_tool
subtractive_curation
importance_tagging
```

不测：

```text
content_dedup
chunk_neighbors
```

因为它们继续作为 runtime controls。

---

# 3. State split

```text
ADV_LIVE256_V2
seed=2225
```

每 component：

```text
256 natural candidate-bearing states
```

来自：

```text
H_-m / reduced runtime occupancy
```

按 `I_name` 分层抽：

```text
low-positive
medium
high
```

不能只挑 top influence。

---

# 4. Fork/replay

同一 `xi_t`：

```text
S branch:
execute reduced/student action a_S

T branch:
execute full-view teacher action a_T
```

之后两边都交还给：

```text
同一个 continuation policy
同一个 runtime config
同一个 remaining budget
```

绝对禁止 teacher 继续接管。

---

# 5. 8 卡 = 4 components × 2 horizons

## GPU0
```text
evidence_graph K=4
256 states
```

## GPU1
```text
evidence_graph K=8
256 states
```

## GPU2
```text
verify_tool K=4
256
```

## GPU3
```text
verify_tool K=8
256
```

## GPU4
```text
subtractive_curation K=4
256
```

## GPU5
```text
subtractive_curation K=8
256
```

## GPU6
```text
importance_tagging K=4
256
```

## GPU7
```text
importance_tagging K=8
256
```

每张卡完成主 cell 后顺序执行：

```text
same-action replay noise shard
```

---

# 6. 主 Utility

优先真实 downstream 指标：

```text
Δ curated recall
Δ trajectory recall
Δ final recall
Δ reward
Δ evidence coverage
Δ calls / turns / latency
```

HF continuation logprob：

```text
只作解释
不得作为主 utility
```

---

# 7. Terminal confirm

聚合 K4/K8 后，对满足：

```text
K4 与 K8 同方向
mean T-S > replay noise
```

的 top 2 component：

```text
各选 128 states
fork to terminal / remaining max budget
```

重新调度已空闲 GPU：

```text
GPU0-3 top1 terminal 分4 shards
GPU4-7 top2 terminal 分4 shards
```

如果只有一个 component 过 K4/K8：

```text
8 卡全做该 component terminal 分片
```

---

# 8. Value Gate

定义 component-level：

```text
A_m(K) = E[U_T(K)-U_S(K)]
```

### `VALUE_POSITIVE`

要求：

```text
K4/K8 同方向
terminal 不反转
mean advantage > replay noise
paired bootstrap 非明显负
```

### `LOCAL_ONLY`

```text
K4 正但 K8/terminal 消失
```

### `BEHAVIOR_ONLY`

```text
policy disagreement 明显，但 utility≈0
```

### `HARMFUL_TEACHER`

```text
T branch 稳定更差
```

---

# 9. 给 H20 的作用

H20 Clean setting 若 Evidence Graph clean gap 不成立：

只允许从本机：

```text
VALUE_POSITIVE
```

组件中选一个 secondary clean target。

禁止：

```text
按 Influence rank 选
按旧 short-horizon score 选
```

---

# 10. 输出

```text
ADVANTAGE_PER_STATE.jsonl
ADVANTAGE_BY_COMPONENT_K.csv
REPLAY_NOISE.md
TERMINAL_CONFIRM.csv
TEACHER_ACTION_VALUE_MAP.md
outputs/scape_prestage_v5/H1002_VALUE_HANDOFF.json
RUN_MANIFEST.json
STATUS_LIVE.md
SHA256SUMS
```

Handoff 字段：

```json
{
  "evidence_graph": "VALUE_POSITIVE|LOCAL_ONLY|BEHAVIOR_ONLY|HARMFUL_TEACHER",
  "verify_tool": "...",
  "subtractive_curation": "...",
  "importance_tagging": "...",
  "recommended_secondary_clean_target": null
}
```
