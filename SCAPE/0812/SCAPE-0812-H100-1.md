# H100-1 下一步：Fresh Contribution Confirmation

## 角色

H100-1 不再重复已完成的 CAL200 10-component LOO。

本机下一任务：

> **用 released Harness-1 checkpoint + 当前可用 local BM25 compat，在完全 query-disjoint 的 fresh split 上确认最重要 component 的 System Contribution。**

官方 Chroma 若仍无 credential，不等待；继续 local compatibility 线，但严格标注。

---

# 0. Git barrier

在开始前检查：

```text
/mnt/songzijun/Capability_Evolution/SCAPE/GIT_SYNC_H100_READY
```

若不存在：

1. 不执行 git checkout / reset / merge；
2. 可以继续读取旧结果；
3. 等 H100-3 完成 shared snapshot；
4. barrier 出现后进入独立 worktree：

```text
/mnt/songzijun/Capability_Evolution/SCAPE-wt-h100-1
branch exp/h1001-contribution-confirm
```

---

# 1. 先验证旧结果完整性

检查：

```text
outputs/h100_1_contribution/
```

要求：

```text
10 component rows
n=200 each
errors=0
RUN_MANIFEST.json
COMPONENT_CONTRIBUTION.json
SHA256SUMS
```

若只是报告没写齐，先补报告，不重跑 CAL200。

---

# 2. 冻结 Fresh Confirm Split

创建：

```text
BCP_CONFIRM400
seed = 1102
n = 400
```

必须与以下集合 query-disjoint：

```text
H100-1 CAL200 seed1101
H100-2 新 REPL200
H20 LOCAL_CAL64
旧 SCOPE finalization100 / fresh200（能拿到 manifest 时一并排除）
```

输出：

```text
manifests/bcp_confirm400.json
manifests/SPLIT_AUDIT.md
```

---

# 3. 主运行条件

固定：

```text
model = pat-jj/harness-1 released checkpoint
backend = local_bm25_compat
temperature = 0
do_sample = false
top_p = 1
same max turns/context/tool budget across all cells
```

禁止修改 prompt/budget 来救某个 component。

---

# 4. 8 卡并行任务

每卡一个主 cell，全部使用同一 400q manifest。

## GPU0
顺序：

```text
C0 full_harness / CONFIRM400
C0b full_harness replay 40q parity
```

## GPU1

```text
C1 - evidence_graph / CONFIRM400
完成后做 per-tool stratified metrics
```

## GPU2

```text
C2 - subtractive_curation / CONFIRM400
完成后统计 curate add/remove/turnover
```

## GPU3

```text
C3 - importance_tagging / CONFIRM400
完成后统计 importance-bearing curate behavior
```

## GPU4

```text
C4 - auto_populate_first_search / CONFIRM400
完成后统计 first-search / first-curate latency
```

## GPU5

```text
C5 - chunk_neighbors / CONFIRM400
标记为 runtime/hybrid control，不自动列为 distillation candidate
```

## GPU6

```text
C6 - content_dedup / CONFIRM400
标记为 deterministic-runtime control
```

## GPU7

```text
C7 - adaptive_rerank_instruction / CONFIRM400
完成后额外跑：
C8 - sentence_compress / CONFIRM200 control
```

若 GPU7 时间不足，C8 可取消；不得影响主七格。

---

# 5. 指标

主 quality：

```text
curated recall
trajectory recall
final-answer recall
reward / benchmark-native quality
```

cost：

```text
turns
tool calls
rendered tokens
latency
state operations
```

paired：

```text
full vs -m
W/L/T
paired bootstrap 95% CI
```

生成：

```text
outputs/h100_1_contribution_confirm/
  CONTRIBUTION_CONFIRM.csv
  CONTRIBUTION_CONFIRM.json
  CONTRIBUTION_CONFIRM.md
  RUNTIME_COST_CONFIRM.csv
  RUN_MANIFEST.json
  STATUS_LIVE.md
  SHA256SUMS
```

---

# 6. 与旧 CAL200 比较

每个 component：

```text
sign_cal200
sign_confirm400
effect_cal200
effect_confirm400
stable?
```

分类：

```text
CONFIRMED_POSITIVE
WEAK_POSITIVE
UNSTABLE
NEUTRAL
NEGATIVE
RUNTIME_CONTROL
```

不要再用全指标简单求和作为唯一排序。

---

# 7. Official Chroma branch

每轮开跑前只检查一次：

```text
OPENAI_API_KEY
CHROMA_API_KEY
CHROMA_DATABASE
```

若三者都存在：

优先新增一个**独立** official job：

```text
full
-evidence_graph
-subtractive_curation
-importance_tagging
-auto_populate_first_search
```

先做 smoke20 -> official100。

不要立刻跑全 10 component。

若凭证不存在：

```text
OFFICIAL_CHROMA_BLOCKED=true
```

不要反复轮询或等待。

---

# 8. 最终 Decision

本机只回答：

> “哪些 component 的 contribution 在 fresh split 上稳定？”

不运行 training，不决定最终 candidate。

把结果提供给：

```text
H20 CANDIDATE_SELECTION_V2
H100-4 influence confirmation
```
