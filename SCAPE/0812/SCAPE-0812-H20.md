# H20 下一步：Canonical Integration + True SCAPE OPD + H100-Gated Training

## 角色

H20 是唯一长期训练节点和 canonical integration 节点。

最新记录说明：

```text
旧 provisional A/B:
  auto_populate_first_search
  verify_tool

Gate S:
  FAIL / FAIL
```

该线归档。

同时记录明确写出：

```text
真 SCAPE same-state tool-token OPD = 未完成
Gate L 仍是 SCOPE-OPD proxy
```

因此 H20 现在最重要的工作不是“等待 H100”，而是先把真正 SCAPE training stack 做完。

---

# Part A. Git canonicalization

## A1. Snapshot H20 tree

在：

```text
/data/ppnm/Capability_Evolution/SCAPE
```

保存：

```bash
git status -sb
git diff --stat
git diff
git remote -v
git rev-parse HEAD
```

写：

```text
docs/H20_PRE_SYNC_AUDIT.md
```

建立：

```text
sync/h20-20260812
```

只提交：

```text
scape/
scripts/
configs/
tests/
docs/
small manifests
dependency files
```

禁止提交：

```text
outputs
checkpoints
models
BM25 index
logs
secrets
```

push。

如果 GitHub auth / remote 不可用：

```text
git bundle create artifacts/git/scape-h20-20260812.bundle --all
```

写 `GITHUB_SYNC_BLOCKED.md`。

---

## A2. Integrate H100 snapshot

检测：

```text
origin/sync/h100-20260812
```

存在后：

```text
integration/scape-20260812
```

合并原则：

```text
H20:
  canonical framework/training/eval

H100:
  contribution/influence/scorer/experiment scripts
```

发生冲突时，不允许简单 `--ours` / `--theirs` 整目录覆盖。

逐文件审查。

必须跑：

```text
pytest -q
python scripts/preflight_scape.py
```

最终写：

```text
docs/CANONICAL_COMMIT.md
```

并 push integration branch。

如果仓库有 PR workflow：

```text
开 draft PR
```

否则在确认 main 可 fast-forward 后更新 main。

---

# Part B. 完成真正 SCAPE OPD（不依赖 H100 结果）

## B1. Canonical path

最终训练代码必须明确走：

```text
scape/state/snapshot.py
scape/rendering/dual_view.py
scape/training/tool_mask.py
scape/training/tool_opd.py
scape/training/teacher.py
```

禁止 canonical experiment 再调用：

```text
SCOPE train_opd
smoke_opd_vllm_hf
KEEP/SKIP
operation CE
ModuleRetirementGate
```

允许旧代码存在于：

```text
legacy/
```

但主 experiment manifest 必须断言：

```text
legacy_scope_path_used = false
```

---

## B2. True same-state collection

实现：

```text
Student rollout under H_-m
       ↓
real environment xi_t
       ↓
save structured snapshot
       ↓
render reduced view r_-m(xi_t)
render full view r_F(xi_t)
       ↓
same model / lagged teacher score
       ↓
Student executes own action
```

必须测试：

```text
teacher_does_not_step_environment
no_future_observation
same_snapshot_hash
full/reduced_only_differ_by_harness_view
```

---

## B3. Tool-token mask

对 Harness-1 action interface：

```text
fan_out_search
search_corpus
grep_corpus
read_document
review_docs
curate
verify
end_search
```

mask：

```text
tool name
argument keys
argument values
optional short end_search reason
```

不训练：

```text
tool observation
retrieved docs
renderer text
system prompt
Harness working-memory text
teacher CoT
```

建立 token alignment audit：

```text
100 sampled calls
100% tool span parsable
invalid span = 0
```

---

## B4. True token distribution

主 V0：

```text
uniform tool-token KL
+ light anchor KL
```

不先上：

```text
teacher confidence
student uncertainty
step divergence
```

这些只做后续 ablation。

需要记录：

```text
D_pre
D_post
name KL
arg-key KL
arg-value KL
invalid-call rate
```

---

# Part C. 8 卡工程/Smoke 阶段

在 H100 candidate 未确定前，只做 plumbing smoke，不做正式能力结论。

推荐两组 TP/训练 group：

```text
Group A = GPU0-3
Group B = GPU4-7
```

## GPU0-3

顺序：

```text
P0 snapshot collection smoke 16q
P1 full/reduced dual-view score
P2 64-state uniform tool-token KL micro-overfit
P3 held-out 32-state divergence eval
```

component 仅使用：

```text
evidence_graph
```

作为 plumbing target。

**不得把此 smoke 写成 candidate 科学结果。**

## GPU4-7

顺序：

```text
Q0 sampled-action CE baseline on same 64 states
Q1 full-response KL baseline on same states
Q2 off-policy full-Harness trace baseline on matched update tokens
Q3 closed-loop 16q syntax/serve smoke
```

目标：

```text
证明三条训练路径在代码层真实不同
```

完成后生成：

```text
outputs/true_scape_pipeline_smoke/
  PIPELINE_AUDIT.md
  TOOL_MASK_AUDIT.md
  SAME_STATE_AUDIT.md
  LOSS_PATH_AUDIT.md
```

---

# Part D. 等 H100 结果后 Candidate V2

读取：

```text
imports/h100_1/CONTRIBUTION_CONFIRM.json
imports/h100_2/LOO_REPLICATION_V2.json
imports/h100_3/REAL_INFLUENCE_BY_COMPONENT.json
imports/h100_4/CANDIDATE_RECOMMENDATION_FOR_H20.json
```

若 H100/H20 没有共享 filesystem：

只同步小型 JSON/CSV/MD；
不要把大 outputs 推进 Git。

生成：

```text
outputs/scape_prestage_v2/CANDIDATE_SELECTION_V2.json
```

选择 top 2。

旧：

```text
auto_populate / verify
```

没有优先权。

---

# Part E. Stage L — True Learnability

假设最终：

```text
Candidate A
Candidate B
```

对每个候选收集：

```text
512
2k
8k
```

real same-state paired states。

## GPU0-3：Candidate A

顺序：

```text
A-512 seed42
A-2k seed42
A-8k seed42
A-heldout

A-2k seed43
A-heldout seed43
```

每个 sample-size 实验必须从同一 base model 独立起跑。

## GPU4-7：Candidate B

同样：

```text
B-512 seed42
B-2k seed42
B-8k seed42
B-heldout

B-2k seed43
B-heldout seed43
```

---

# Part F. Gate L

进入 single-component migration 必须：

```text
held-out tool divergence 明显下降
2 seeds 同方向
invalid tool call 不恶化
closed-loop smoke 不 collapse
```

如果 candidate：

```text
uniform KL fail
```

最多再试一次：

```text
teacher-confidence weighting
```

仍失败：

```text
CURRENTLY_NOT_LEARNABLE
```

换下一个 candidate。

禁止连续多轮 objective rescue。

---

# Part G. Single-Component Migration

先只跑 Candidate A。

必须有：

```text
S0 theta0 + H_full
S1 theta0 + H_-A
S2 thetaA + H_-A
S3 thetaA + H_full
```

## GPU0-3

```text
SCAPE-A seed42
four-grid eval
CCR / N_post
```

## GPU4-7

```text
SCAPE-A seed43
four-grid eval
CCR / N_post
```

若两 seed 都 positive，再：

```text
空闲 group 跑 seed44
另一 group 跑 same-state action CE ablation
```

---

# Part H. Gate S

Strong:

```text
J(thetaA,H_-A) > J(theta0,H_full)
C(H_-A) < C(H_full)
```

Acceptable:

```text
non-inferior quality
+
material cost reduction
```

Fail:

```text
仅 tool agreement/KL 变好
但闭环 task gap 没补回
```

失败不进入 Stage M。

---

# Part I. RL + OPD

只有 distillation-only 已经形成明显 positive compensation 后，才允许：

```text
RL + tool-token OPD
```

目的：

```text
从 quality-preserving
推进到 outperform original full harness
```

不要用 RL 来拯救一个连 capability migration 都没建立的 candidate。

---

# Part J. Multi-component

只有至少一个 single component Gate S PASS。

若 A/B 都 pass：

```text
GPU0-3:
  sequential A->B seed42
  -> joint A+B seed42

GPU4-7:
  sequential B->A seed42
  -> guided annealing seed42
```

再根据首轮结果扩 seed43。

---

# Part K. Official Chroma

每次进入新大阶段前只检查一次凭证。

如果可用：

```text
优先把最终 A、B 和 full baseline
在 official Chroma 上重测
```

不需要先补所有 10 component official LOO。

如果仍缺：

```text
论文/报告标记 LOCAL_COMPAT_ONLY
```

不等待。

---

# Part L. Result Record

所有旧 provisional 结果继续保留，但新增明确分区：

```markdown
## Canonical True-SCAPE Line
```

每个新条目必须包含：

```text
canonical commit
upstream Harness-1 commit
model revision
retrieval backend
component mask
snapshot schema version
tool-mask version
teacher backend
loss implementation
legacy_scope_path_used=false
```

这是避免未来把 proxy 与 true SCAPE 再次混写的关键。
