# SCAPE H100-2 下一步：Candidate-B Live Utility Validation

## 任务定位

上一轮 `UTILITY_STATE256` 排名：

```text
subtractive_curation > importance_tagging > verify_tool
```

但总体 decision 仍是：

```text
Behavior-only
```

且 replay noise 仍是 deterministic artifact 的 0，不是 live 实测。

因此本机下一步：

> 用真实 fork/replay runner 在 fresh states 上重新验证三候选 utility，并实测 replay noise。

这是 Candidate B 冻结前最后一个 utility gate。

---

## 0. Repo / 环境

```text
/mnt/songzijun/Capability_Evolution/SCAPE-wt-h100-2
```

分支：

```text
exp/h1002-live-utility-v2
```

开始：

```bash
git status -sb
git fetch origin
pytest -q
```

GPU-heavy env 放 `/opt`。

---

## 1. 候选

只测：

```text
subtractive_curation
importance_tagging
verify_tool
```

---

## 2. Fresh Live Split

冻结：

```text
UTILITY_LIVE256
seed=2214
n=256 candidate-bearing states/component
```

从 fresh query split 获取。

每 state：

```text
xi_t
a_S = reduced-view action
a_T = full-view action
```

仅当：

```text
tool-name differs
或
argument divergence above threshold
```

进入 utility fork。

---

## 3. Live Fork

```text
Branch S:
  execute a_S
  same policy continues K

Branch T:
  execute a_T
  same policy continues K
```

运行：

```text
K=2
K=4
```

禁止 Full Harness 后续 takeover。

---

## 4. Live Replay Noise

增加：

```text
Branch N1:
  execute a_S
  continue K

Branch N2:
  execute same a_S
  continue K
```

实测：

```text
replay_noise
```

所有 T-S utility 都要相对 replay noise 报 effect。

---

## 5. Utility 指标

只用可客观计算项：

```text
curated evidence gain
useful unique docs
redundancy change
evidence coverage
verified/unsupported claim status（若可靠）
tool/search cost
```

---

## 6. 8 卡并行

### GPU0
```text
subtractive K=2
```

### GPU1
```text
subtractive K=4
```

### GPU2
```text
importance K=2
```

### GPU3
```text
importance K=4
```

### GPU4
```text
verify K=2
```

### GPU5
```text
verify K=4
```

### GPU6
```text
same-action live replay noise
K=2 + K=4
```

### GPU7
```text
aggregation + stratification
natural/targeted
name-change/args-only
early/mid/late
```

---

## 7. Gate

### STRONG_B
要求：

```text
real influence positive
fresh contribution不矛盾
live utility > replay noise
K2/K4 direction一致
```

### WEAK_B
utility 仅略高于 replay noise：

```text
允许进入 H20 micro-learnability
不直接升主训练
```

### BEHAVIOR_ONLY
若：

```text
influence positive
utility <= replay noise
```

不推荐主 B。

### CONDITIONAL_RUNTIME
若 verify：

```text
natural weak
targeted strong
```

作为 conditional runtime。

---

## 8. 输出

```text
outputs/h100_2_candidate_b_live_utility/
  LIVE_UTILITY_PER_STATE.jsonl
  LIVE_UTILITY_SUMMARY.csv
  LIVE_REPLAY_NOISE.csv
  SUBTRACTIVE_LIVE_UTILITY.md
  IMPORTANCE_LIVE_UTILITY.md
  VERIFY_LIVE_UTILITY.md
  CANDIDATE_B_LIVE_DECISION.json
  CANDIDATE_B_LIVE_DECISION.md
  RUN_MANIFEST.json
  SHA256SUMS
```

handoff：

```text
outputs/scape_prestage_v3/H1002_CANDIDATE_B_LIVE_HANDOFF.json
```

---

## 9. 禁止事项

```text
不训练
不重跑全 LOO
不重跑 real influence
不把 short-horizon utility 当 final success
不再假设 replay_noise=0
```
