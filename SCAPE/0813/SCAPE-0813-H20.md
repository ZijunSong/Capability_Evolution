# H20 下一步指令：True SCAPE Evidence-Graph Migration

## 目标

H20 现在是主线机器。

旧 provisional：

```text
Qwen2.5-7B + BM25
auto_populate
verify_tool
SCOPE-OPD proxy
```

已经完成且 Gate S FAIL。

全部归档，不再救。

当前主线：

```text
Candidate A = evidence_graph
```

因为它同时具备：

```text
fresh contribution positive
independent replication positive
real influence rank #1
H100-4 confirm positive
```

本机现在立即开始真正 SCAPE：

```text
same-environment-state
+
dual-view
+
tool-token KL
```

无需等待 H100-1/2/4 新一轮结果。

Candidate B 暂不冻结，等待：

```text
H100-2 candidate-B utility
H100-4 verify confirm
```

---

# Part A. Canonical Git Integration

## A1. 当前 H20 repo

```text
/data/ppnm/Capability_Evolution/SCAPE
```

先：

```bash
git status -sb
git log --oneline -10
git remote -v
gh auth status
```

## A2. 获取 H100 代码

目标至少包含 H100 snapshot：

```text
0f0934bd9f7a985af747e18dda9c2c666a9c24ba
```

以及：

```text
real HF continuation scorer
snapshot schema
dual-view renderer
influence scripts/tests
```

如果 remote 上已有 branch：

```text
fetch + merge/cherry-pick
```

如果未 push：

不要猜。

要求 H100-3 的 `H100_GITHUB_HANDOFF.md` / branch 后再合并。

## A3. Canonical branch

建立：

```text
integration/true-scape-evidence-graph
```

跑：

```bash
pytest -q
```

必须新增并通过：

```text
test_snapshot_no_future_information
test_same_snapshot_dual_view
test_teacher_does_not_step_environment
test_tool_name_span
test_argument_key_span
test_argument_value_span
test_end_search_span
test_legacy_scope_path_not_used
```

提交并 push。

---

# Part B. 模型与 Harness

Canonical mechanism experiment 要与 H100 pre-stage 尽量同模型：

```text
model = pat-jj/harness-1 released checkpoint
harness = Harness-1
component = evidence_graph
retrieval = local_bm25_compat
```

不要再用 Qwen2.5-7B 作为主 SCAPE evidence。

如果 full-parameter training 不现实：

```text
使用 LoRA/PEFT
```

但必须：

```text
base checkpoint 相同
teacher/student checkpoint provenance 明确
最终可 merge / serve
```

---

# Part C. 真实 Same-State 数据

Student 在：

```text
H_-evidence_graph
```

真实 rollout。

每 decision state 保存：

```text
xi_t structured snapshot
snapshot_hash
student reduced view
teacher full view
student executed action
tool span offsets
tool-name tokens
argument-key tokens
argument-value tokens
```

Full Harness：

```text
只 score 当前 state
不能执行 future trajectory
```

---

# Part D. Stage L 数据规模

冻结：

```text
EG_TRAIN_8K
EG_VALID_1K
EG_TEST_1K
```

query-disjoint 优先；
若 state 数来自同 query 多 turn：

必须按 query split，不能按 state random split。

从 train pool 截取：

```text
512
2k
8k
```

---

# Part E. 首版 Loss

V0：

```text
L =
    uniform tool-token KL
    + light anchor KL
```

mask：

```text
tool name
argument keys
argument values
```

不训练：

```text
retrieved docs
observations
renderer text
system prompt
Harness text
teacher CoT
```

先不使用：

```text
teacher confidence
student uncertainty
step divergence
```

---

# Part F. 8 卡 Stage L

如果 20B + LoRA 可以单 H20 稳定训练：

## GPU0

```text
EG L512 seed42
→ heldout
→ EG L2K seed42
→ heldout
→ EG L8K seed42
→ heldout
```

每个 sample size 从同一个 base 独立初始化。

## GPU1

```text
EG L512 seed43
→ L2K seed43
→ L8K seed43
→ heldout
```

## GPU2

```text
EG L2K seed44
→ EG L8K seed44
→ heldout
```

## GPU3

same-state sampled-action CE：

```text
2K
→ 8K
```

## GPU4

same-state full-response KL：

```text
2K
→ 8K
```

## GPU5

off-policy Full-Harness trace distillation：

```text
matched tasks
matched approximate update tokens
```

## GPU6

tool-name-only KL：

```text
2K
→ 8K
```

## GPU7

arguments-only KL：

```text
2K
→ 8K
```

如果单卡不能稳定训练：

固定改成 4 个 TP/训练组，每组 2 GPU：

```text
0-1 main seed42
2-3 main seed43
4-5 action-CE baseline
6-7 full-response/off-policy sequential
```

并把 name-only/args-only 延后。

---

# Part G. Stage L 统计

每 cell：

```text
D_pre
D_post
I/name KL on heldout
arg-key KL
arg-value KL
tool-name agreement
exact tool-call agreement
invalid tool rate
closed-loop smoke16
```

定义：

```text
L_m = 1 - D_post / (D_pre + eps)
```

但不能只看一个标量。

---

# Part H. Gate L

PASS：

```text
两 seed held-out divergence 都下降
2k/8k 不比 512 系统性恶化
invalid tool 不上升
smoke16 不 collapse
```

如果 uniform KL FAIL：

只允许一次：

```text
根据 H100-3 attribution
尝试 name/args weighting
```

仍 FAIL：

```text
CURRENTLY_NOT_LEARNABLE
```

停止 Evidence Graph full migration。

---

# Part I. Stage S — Single-Component Migration

Gate L PASS 后立即做：

```text
S0 theta0 + H_full
S1 theta0 + H_-graph
S2 thetaEG + H_-graph
S3 thetaEG + H_full
```

测试集必须与 Stage L train/valid query-disjoint。

至少：

```text
n >= 200 tasks
```

主指标：

```text
curated recall
trajectory recall
final recall
reward
```

runtime：

```text
graph state ops
graph rendered tokens
context tokens
tool calls
latency
```

---

# Part J. Evidence-Graph Retirement 不是“一刀切”

结合 H100-1 decomposition，测试：

```text
H_full
H_graph_off
H_graph_state_only
H_graph_state_plus_minimal_render
```

训练后重新扫：

```text
thetaEG + each runtime
```

目标可能不是：

```text
delete graph entirely
```

而是：

```text
external graph state retained
+
graph renderer/controller reduced
+
graph-aware decisions internalized
```

这反而更符合 SCAPE。

---

# Part K. Candidate B

等待两个文件：

```text
H100-2:
  CANDIDATE_B_RECOMMENDATION.json

H100-4:
  H1004_VERIFY_HANDOFF.json
```

然后在：

```text
importance_tagging
verify_tool
subtractive_curation
```

中选 1 个 B。

不要同时启动三个。

只有 A Gate L 明确结果后再开 B Stage L，以防 true-SCAPE pipeline 仍有基础问题。

---

# Part L. Stage M

只有：

```text
A Gate S PASS
```

后才考虑。

如果 A PASS、B 也 PASS：

```text
sequential A->B
joint dropout A+B
guided annealing
```

否则不做。

---

# Part M. 必须生成的核心产物

```text
outputs/true_scape_evidence_graph/
  DATA_AUDIT.md
  TOOL_MASK_AUDIT.md
  STAGE_L_CURVE.csv
  STAGE_L_REPORT.md
  BASELINE_COMPARISON.md
  FOUR_GRID_STAGE_S.md
  CCR_EVIDENCE_GRAPH.json
  RUNTIME_RECOMPOSITION.md
  PROBE_PREDICTION_CHECK.md
```

其中 `PROBE_PREDICTION_CHECK.md` 必须直接回答：

```text
Pre-stage:
  Contribution positive?
  Influence positive?

Post-stage:
  Learnability positive?
  Retirement/Hybrid success?

因此：
  Contribution–Influence 是否预测对了？
```

这是当前论文最关键的解释性闭环。
