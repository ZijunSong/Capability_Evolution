# SCAPE H100-1 下一步：Evidence-Graph Hybrid Target Decomposition

## 任务定位

上一轮已经完成 `evidence_graph` 的 G0–G4 placement decomposition，并得到：

```text
G3 ≈ G4
G2 只保留少量 utility
```

因此 `evidence_graph` 不再作为“整组件完全内化”的目标。更合理的 placement 是：

```text
external graph state 保留
+
graph-aware semantic decision -> weights
+
renderer/controller 后续 slim
```

H20 对完整 `evidence_graph` 的 true-SCAPE Stage L 已双次 FAIL。本机不要再做普通 LOO，也不要启动训练。

本机下一步只回答：

> G2→G3 的 policy influence 是否显著，而 G3→G4 是否很小？

若成立，就能把 graph 的“可迁移部分”精确限定成 minimal-render 驱动的 graph-aware decision。

---

## 0. Repo / 环境

```text
/mnt/songzijun/Capability_Evolution/SCAPE-wt-h100-1
```

建议分支：

```text
exp/h1001-graph-hybrid-influence
```

开始前：

```bash
git status -sb
git fetch origin
pytest -q
```

GPU Python 必须在 `/opt`。

模型：

```text
pat-jj/harness-1
```

scorer：

```text
HF continuation-logprob
```

禁止使用旧 offline stub。

---

## 1. Same-State Four-View Probe

从新的 reduced-runtime rollout 采集真实：

```text
xi_t
```

对同一个 `snapshot_hash` 构造：

```text
V1 = GRAPH_OFF
V2 = GRAPH_STATE_ONLY
V3 = GRAPH_STATE_PLUS_MINIMAL_RENDER
V4 = GRAPH_FULL_RENDER
```

所有 view：

```text
只 render + score 当前 xi_t
不执行 future trajectory
不加入未来 observation
```

---

## 2. 数据规模

冻结：

```text
GRAPH_HYBRID_INF128
seed=1115
n_queries=128
max_states_per_query=16
```

与既有 graph decomp / influence split 尽量 query-disjoint。

保存：

```text
snapshot_hash
turn_id
student executed tool
P_tool(V1..V4)
argument-token scores
```

---

## 3. 核心统计

计算：

```text
I_12 = OFF -> STATE_ONLY
I_23 = STATE_ONLY -> MINIMAL_RENDER
I_34 = MINIMAL_RENDER -> FULL_RENDER
```

分别报告：

```text
tool-name JS
arg-key KL/JS
arg-value KL/JS
tool-name disagreement
exact-call disagreement
```

重点：

```text
I_23 >> I_34 ?
```

---

## 4. 8 卡并行

### GPU0
```text
V1 GRAPH_OFF
```

### GPU1
```text
V2 GRAPH_STATE_ONLY
```

### GPU2
```text
V3 GRAPH_STATE_PLUS_MINIMAL_RENDER
```

### GPU3
```text
V4 GRAPH_FULL_RENDER
```

### GPU4
```text
argument attribution:
doc_ids / claim relation / termination reason
```

### GPU5
```text
turn/tool stratification:
read / review / curate / verify / end
early / middle / late
```

### GPU6
```text
null:
V3 vs V3
field-order-only perturbation
```

### GPU7
```text
replay parity + aggregation
```

若 scorer 只能 TP=2：

```text
GPU0-1: V1 -> V3
GPU2-3: V2 -> V4
GPU4-5: argument attribution
GPU6-7: null + replay + aggregation
```

---

## 5. 判定

### HYBRID_TARGET_CONFIRMED
若：

```text
I_23 > null
I_23 >= I_34
且上一轮 G3 quality ≈ G4
```

则：

```text
graph state 保留
minimal render 足够
graph-aware decision 作为未来 narrow migration target
```

### RUNTIME_DOMINATED
若：

```text
I_12 最大
I_23/I_34 很弱
```

停止参数内化。

### FULL_RENDER_DEPENDENT
若：

```text
I_34 仍大
```

完整 renderer 仍是重要 scaffold，不主张 slim。

---

## 6. 输出

```text
outputs/h100_1_graph_hybrid_influence/
  GRAPH_HYBRID_INF_PER_STATE.jsonl
  GRAPH_HYBRID_INF_SUMMARY.csv
  GRAPH_HYBRID_TARGET.md
  ARGUMENT_ATTRIBUTION.md
  NULL_CONTROL.md
  RUN_MANIFEST.json
  SHA256SUMS
```

handoff：

```text
outputs/scape_prestage_v3/H1001_GRAPH_HYBRID_HANDOFF.json
```

---

## 7. 禁止事项

```text
不训练
不再跑 full evidence_graph Stage L
不扫 lr / LoRA
不重跑全 component LOO
不声称 official Chroma parity
```
