# H100-4 下一步指令：verify_tool Real Influence CONFIRM128

## 目标

H100-4 已经确认：

```text
evidence_graph
importance_tagging
subtractive_curation
```

当前最明确的缺口是：

```text
verify_tool
```

因为 H100-3 released-checkpoint real influence 中：

```text
verify_tool = rank #2
```

但 H100-4 上一轮没有覆盖它。

所以本机本轮第一优先级只有一个：

> 独立 CONFIRM128 `verify_tool`。

---

# 0. Repo

```text
/mnt/songzijun/Capability_Evolution/SCAPE-wt-h100-4
branch exp/h1004-verify-confirm
```

先：

```bash
pytest -q
```

使用 canonical HF continuation-logprob scorer。

禁止回到 offline stub。

---

# 1. 数据

创建：

```text
VERIFY_INF_CONFIRM128
seed = 4414
n = 128
```

与：

```text
H100-3 REAL_INF64
H100-4 previous CONFIRM128
```

query-disjoint。

每题最多：

```text
16 natural decision states
```

---

# 2. 4 卡并行

## GPU0

```text
verify_tool NATURAL CONFIRM128
first half
```

## GPU1

```text
verify_tool NATURAL CONFIRM128
second half
```

合并后要求：

```text
n_queries=128
state hash unique
errors=0
```

## GPU2

targeted verify-eligible states：

```text
最多 512 states
```

筛选条件只允许：

```text
已有 claim
已有 evidence
verify tool legal
存在 conflict / uncertainty / unsupported claim
```

单独标记：

```text
TARGETED=true
```

不与 natural 混写。

## GPU3

null + robustness：

```text
N0 same render
N1 same reduced render
N2 field-order perturbation
replay same 128 states
```

---

# 3. 指标

```text
I_name_raw
I_name_null
I_name_normalized
I_args_raw
tool-name disagreement
verify-routing change rate
args-only change rate
teacher entropy
student entropy
```

同时按：

```text
early/mid/late
natural/targeted
```

分层。

---

# 4. Gate

### CONFIRMED
需要：

```text
natural I above null
H100-3 and H100-4 same sign
effect not concentrated in tiny number of states
```

### RARE_EVENT_ONLY
如果：

```text
natural weak
targeted strong
```

则 verify 更适合：

```text
hybrid conditional runtime
```

而不是 main migration B。

### NOT_CONFIRMED
如果：

```text
natural ~ null
targeted 也弱
```

则不作为 Candidate B。

---

# 5. 完成 verify 后的第二任务

如果 GPU 仍有空闲：

对：

```text
auto_populate_first_search
```

做 128-state argument diagnostic。

原因：

H100-3 中它：

```text
I_name positive
I_args strongly negative
```

需要判断是不是 argument scorer / token alignment artifact。

这只是诊断，不参与当前 A/B 主线。

使用：

```text
GPU0-1 auto_populate rescore
GPU2 argument token alignment audit
GPU3 null/replay
```

---

# 6. 输出

```text
outputs/h100_4_verify_confirm/
  VERIFY_REAL_INF_CONFIRM128.csv
  VERIFY_REAL_INF_CONFIRM128.json
  VERIFY_NATURAL_VS_TARGETED.md
  VERIFY_NULL_REPORT.md
  VERIFY_CANDIDATE_B_DECISION.md
  RUN_MANIFEST.json
  SHA256SUMS
```

并写一个小型 handoff：

```text
outputs/scape_prestage_v2/H1004_VERIFY_HANDOFF.json
```

只包含：

```text
confirmed?
natural influence
targeted influence
event support
recommend_candidate_b?
```
