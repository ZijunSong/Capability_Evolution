# SCAPE H100-4 下一步：Candidate-B Independent Live Confirmation（4×H100）

## 资源约束

**本机只有 4 张 H100。**

所有任务严格按 4 卡设计。

禁止复制其他 H100 的 8 卡队列。

---

## 任务定位

H100-4 已完成：

```text
evidence_graph / importance_tagging / subtractive_curation influence confirm
verify_tool independent confirm
```

当前最有价值的是独立验证 H100-2 的 utility 排名：

```text
subtractive_curation > importance_tagging > verify_tool
```

因此本机只比较：

```text
subtractive_curation
importance_tagging
```

verify 已确认 influence，继续作为 conditional-runtime challenger，不再重复 influence。

---

## 0. Repo / 环境

```text
/mnt/songzijun/Capability_Evolution/SCAPE-wt-h100-4
```

分支：

```text
exp/h1004-b-utility-confirm
```

开始：

```bash
git status -sb
git fetch origin
pytest -q
```

GPU Python 必须使用：

```text
/opt/scape-hf-scorer/bin/python
```

或其他 `/opt` 下有效 env。

禁止 `/mnt` JuiceFS Python env。

---

## 1. Fresh Split

冻结：

```text
B_UTILITY_CONFIRM128
seed=4424
```

与 H100-2 `UTILITY_LIVE256` query-disjoint。

目标：

```text
128 candidate-bearing states/component
```

---

## 2. 4 卡任务

### GPU0
```text
subtractive_curation
K=2
128 states
```

### GPU1
```text
subtractive_curation
K=4
128 states
```

### GPU2
```text
importance_tagging
K=2
-> K=4
顺序执行
```

### GPU3
```text
live same-action replay noise
+
null controls
+
aggregation
```

如果单任务需要多卡加载：

改成两组 TP=2：

```text
GPU0-1:
subtractive K2 -> K4

GPU2-3:
importance K2 -> K4
-> replay noise
```

绝不超过 4 卡。

---

## 3. 指标

与 H100-2 完全同口径：

```text
T-S utility
curated evidence gain
useful unique docs
redundancy
coverage
tool/search cost
replay noise
```

必须报告：

```text
effect / replay_noise
```

---

## 4. 判定

### SUBTRACTIVE_CONFIRMED_AS_B
如果：

```text
subtractive > replay noise
K2/K4 consistent
且 >= importance
```

### IMPORTANCE_OVERTAKES
如果：

```text
importance 在独立 split 更稳
```

则 H20 tournament 二者同优先级。

### BOTH_WEAK
如果：

```text
两者 <= replay noise
```

H20 只做 512/2K micro-learnability，不扩 8K。

---

## 5. 输出

```text
outputs/h100_4_b_utility_confirm/
  B_UTILITY_CONFIRM_PER_STATE.jsonl
  B_UTILITY_CONFIRM_SUMMARY.csv
  REPLAY_NOISE.csv
  SUBTRACTIVE_VS_IMPORTANCE.md
  H1004_B_UTILITY_HANDOFF.json
  RUN_MANIFEST.json
  SHA256SUMS
```

handoff：

```text
outputs/scape_prestage_v3/H1004_B_UTILITY_HANDOFF.json
```

---

## 6. 余力任务

只有主任务完成后：

```text
verify_tool natural-vs-targeted cost profile
```

不再做 verify influence confirm。

---

## 7. 禁止事项

```text
不训练
不跑 8 卡计划
不重跑 verify CONFIRM128
不重跑全 component map
不声称 official Chroma parity
```
