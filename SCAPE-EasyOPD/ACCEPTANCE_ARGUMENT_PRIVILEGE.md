# ACCEPTANCE_ARGUMENT_PRIVILEGE

importance_tagging is registered as PARTIAL with projected_action_ce schema. Full support smoke remains gated by real valid add/remove support; SUPPORT_GATE_FAIL is the expected behavior if support is insufficient.
