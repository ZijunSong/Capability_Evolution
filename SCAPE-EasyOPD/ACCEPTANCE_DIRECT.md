# ACCEPTANCE_DIRECT

DIRECT evidence_graph acceptance passed: state fork, tool span, query split, shuffled control contract, no privilege flag, and live Harness-1 multi-turn Search (`search_corpus`, `read_document`, final user_text).
