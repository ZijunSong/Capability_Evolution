# Adaptive P/U/rho weighting — Round 2+ only (kept for ablation scaffolding).
scope:
  enabled: true
  schema_version: scope.v3
  capabilities:
    enabled: [duplicate_evidence, premature_stop]
  weighting:
    mode: adaptive
    enabled: true
    min_weight: 0.1
    max_weight: 1.0
  sdi:
    enabled: true
    kl_coef: 0.01
  recovery:
    enabled: false
  rl:
    enabled: false
