name: scope-baseline-opid
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch
      - transformers
      - vllm
      - accelerate
notes: |
  Isolated env for OPID. Never mutate SCOPE bishop environment.
