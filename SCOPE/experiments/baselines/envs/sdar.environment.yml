name: scope-baseline-sdar
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch
      - transformers
      - vllm
notes: |
  Isolated env for SDAR / GRPO-style baselines. Install only in dedicated env.
